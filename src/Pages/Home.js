import Button from 'react-bootstrap/Button';
import React from 'react';

const Home = () => {
    return (
        <div>
            <Button>test button</Button>
        </div>
    );
};

export default Home;